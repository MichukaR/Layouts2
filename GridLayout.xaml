<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppLayouts.GridLayout"
             Title="Grid Layout">
    <ScrollView>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Frame Grid.Row="0" Grid.Column="0" BackgroundColor="#4b1535" Padding="0" >
                
            </Frame>

            <Frame Grid.Row="0" Grid.Column="1" BackgroundColor="#f3c8dd" Padding="0" >
                
            </Frame>

            <Frame Grid.Row="1" Grid.Column="0" BackgroundColor="#d183a9" Padding="0" >
                
            </Frame>

            <Frame Grid.Row="1" Grid.Column="1" BackgroundColor="#7e2a53" Padding="0" >
                <Button Text="Stack Layout" 
                        BackgroundColor="LavenderBlush"
                       HorizontalOptions="Center" 
                       VerticalOptions="Center" 
                       FontSize="24"
                       TextColor="#7e2a53"
                       Clicked="Button_OnClicked"/>
            </Frame>
        </Grid>
            
        
    </ScrollView>
</ContentPage>